name: Close Pull Request
on:
  pull_request_target:
    types: [opened]

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - name: Close Pull
      run: gh pr close --comment "PRs aren't allowed in this repository." --delete-branch
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
